<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoFinal.Views.AyudaPage"
             Title="Ayuda">

    <ScrollView>
        <VerticalStackLayout
            Padding="30"
            Spacing="15">

            <Label
                Text="Ayuda y Soporte"
                FontSize="28"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="{StaticResource Primary}" />

            <Label
                Text="�Necesitas ayuda? Cont�ctanos"
                FontSize="16"
                HorizontalOptions="Center"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,20" />

            <Label
                Text="Preguntas Frecuentes"
                FontSize="20"
                FontAttributes="Bold"
                Margin="0,10,0,10" />

            <Label
                Text="�C�mo registrar una compra?"
                FontSize="16"
                FontAttributes="Bold" />
            <Label
                Text="Ve a 'Gesti�n de Compras' y completa los datos del art�culo adquirido."
                FontSize="14"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,15" />

            <Label
                Text="�C�mo realizar un abono?"
                FontSize="16"
                FontAttributes="Bold" />
            <Label
                Text="Selecciona 'Registro de Abonos' y elige la compra a la que deseas abonar."
                FontSize="14"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,15" />

            <Label
                Text="�C�mo consultar mi historial?"
                FontSize="16"
                FontAttributes="Bold" />
            <Label
                Text="Ve a 'Consultas' para ver todas tus compras y abonos realizados."
                FontSize="14"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,20" />

            <Label
                Text="Informaci�n de Contacto"
                FontSize="20"
                FontAttributes="Bold"
                Margin="0,10,0,10" />

            <Label
                Text="Email:"
                FontSize="16"
                FontAttributes="Bold" />
            <Label
                Text="soporte@highvaluepay.com"
                FontSize="14"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,10" />

            <Label
                Text="Tel�fono:"
                FontSize="16"
                FontAttributes="Bold" />
            <Label
                Text="+506 0000-0000"
                FontSize="14"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,10" />

            <Label
                Text="Horario de Atenci�n:"
                FontSize="16"
                FontAttributes="Bold" />
            <Label
                Text="Lunes a Viernes: 8:00 AM - 6:00 PM"
                FontSize="14"
                TextColor="{StaticResource Gray500}"
                Margin="0,0,0,20" />

            <Button
                Text="Volver al Menu"
                Clicked="OnVolverClicked"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                CornerRadius="10"
                HeightRequest="50" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
